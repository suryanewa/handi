/**
 * Placeholder: real implementation would merge PDFs (e.g. pdf-lib) and upload to storage.
 * For hackathon we return a placeholder URL.
 */
export async function runMergePdfs(_fileUrlsOrBase64: string[]): Promise<string> {
  return 'https://example.com/merged.pdf';
}
